#include <stdio.h>
int main(){
     int size;
     printf("Enter size of array: ");
     scanf("%d",&size);
     int i=0;
     int arr[size];
     for(i=0;i<size;i++){
        printf("Enter Array element: ");
        scanf("%d",&arr[i]);
     }; 
     int j;
     int k;
     for(i=0;i<size;i++){
        for(k=0;k<=i;k++){
          printf("%d ",arr[k]);
        }
        printf("\n");
    }
    return 0;
}
